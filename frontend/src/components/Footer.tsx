export default function Footer() {
  return <footer className="site-footer"><div><p>CreditRisk Predictor</p><small>Predictions are decision support, not a final lending decision.</small></div></footer>;
}
